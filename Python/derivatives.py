import numpy

# Place to put derivative functions.

def FivePointStencil(r1, r2, l1, l2, u1, u2, d1, d2):
  # calculate the gradient at a point.
  # inputs should be a particle class (or something similar)
  # which contain all the dynamical data necessary to compute derivatives
  # and velocity updates. They are the fields.

